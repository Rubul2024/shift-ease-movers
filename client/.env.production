# Keep all JS in external files so the server's Content-Security-Policy (script-src 'self') allows it.
INLINE_RUNTIME_CHUNK=false
# Don't ship source maps (they expose the full source code).
GENERATE_SOURCEMAP=false
